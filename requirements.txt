# Plataforma BI INEP/PND — camada Streamlit v17
# Dependências do motor completo + interface Streamlit.
streamlit>=1.40
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
xlsxwriter>=3.1
openpyxl>=3.1
# Performance: leitura de Excel 5–10x mais rápida (com fallback automático ao openpyxl):
python-calamine>=0.2
# Opcional (gráficos "standalone" extras do motor; o dashboard e o Excel não dependem disto):
plotly>=5.20
kaleido>=0.2.1 
sweetviz>=2.3.0 
tqdm>=4.66.0 

