# Plataforma BI INEP/PND — camada Streamlit v1
# Dependências do motor completo + interface Streamlit.
streamlit>=1.36
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
xlsxwriter>=3.1
openpyxl>=3.1
# Opcional (gráficos "standalone" extras do motor; o dashboard e o Excel não dependem disto):
plotly>=5.20
